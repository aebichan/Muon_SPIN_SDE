# Muon_SPIN_SDE

Numerical lookup tables for the longitudinal muon-spin polarization function $$G_z(t)$$ used in the open-system stochastic spin-dynamics model of muon spin relaxation.

The tables were generated by Monte Carlo sampling of the stochastic spin equation with colored Ornstein--Uhlenbeck noise and a retarded memory/backaction term.

For details of the theory, see the arXiv manuscript:
[Open Quantum System Theory of Muon Spin Relaxation in Materials](https://arxiv.org/abs/2603.23137).

## Available lookup tables

Two lookup tables are currently provided

### v1.0: Static muon table

Release page:

https://github.com/aebichan/Muon_SPIN_SDE/releases/tag/v1.0
The table corresponds to the static-muon case used in the manuscript,

This table assumes

$$\nu_\mu = 0.$$

Therefore, the muon is treated as static, and D_mu_vals represents the quenched/background Gaussian field width $$\Delta_\mu$$.

### Downloading the lookup table

The full lookup table is

```text
dataset_mu_Li_OU_alpha_Ntraj_90k.npz
```

Because the file is larger than GitHub's single-release-asset size limit, it is provided in two parts under the release page:

```text
dataset_mu_Li_OU_alpha_Ntraj_90k.npz.part_aa
dataset_mu_Li_OU_alpha_Ntraj_90k.npz.part_ab
SHA256SUMS.txt
```

Release page:

```text
https://github.com/aebichan/Muon_SPIN_SDE/releases/tag/v1.0
```

After downloading both parts, reconstruct the original `.npz` file with

```bash
cat dataset_mu_Li_OU_alpha_Ntraj_90k.npz.part_* \
  > dataset_mu_Li_OU_alpha_Ntraj_90k.npz
```

Then verify the reconstructed file using

```bash
sha256sum -c SHA256SUMS.txt
```

## v2.0: finite-muon-hopping table

Release page:

https://github.com/aebichan/Muon_SPIN_SDE/releases/tag/v2.0

This table includes a finite grid of muon hopping/decorrelation rates $$\nu_\mu$$. 

Full reconstructed file:

```text
dataset_mu_Li_OU_alpha_numu_Ntraj_90k.npz
```

Split release files:

```text
dataset_mu_Li_OU_alpha_numu_Ntraj_90k.npz.part_aa
dataset_mu_Li_OU_alpha_numu_Ntraj_90k.npz.part_ab
...
dataset_mu_Li_OU_alpha_numu_Ntraj_90k.npz.part_ao
SHA256SUMS_v2.txt
```

Reconstruct:

cat dataset_mu_Li_OU_alpha_numu_Ntraj_90k.npz.part_* > dataset_mu_Li_OU_alpha_numu_Ntraj_90k.npz

Verify:

```bash
sha256sum -c SHA256SUMS_v2.txt
```

Main table shape:

```bash
G_table.shape = (10, 10, 10, 10, 10, 4, 18001)
```

Axis order:

```bash
G_table[
    i_D_mu,
    i_D_Li,
    i_nu_mu,
    i_nu_Li,
    i_alpha,
    i_B_L,
    i_t
]
```
or, in manuscript notation,

$$
G_z =
G_z(
\Delta_\mu,
\Delta_{\rm Li},
\nu_\mu,
\nu_{\rm Li},
\Lambda,
B_L;
t
).
$$

## Quick start (v1.0: Static muon case)

After reconstructing the `.npz` file, run

```bash
python example_load_curve.py
```

This script loads the table, extracts one example (G_z(t)) curve, and saves

```text
example_curve.png
```

## Parameter-name mapping

The parameter names inside the `.npz` file are the names used during numerical table generation. Some of them differ from the notation used in the manuscript. The correspondence is:

| NPZ key      | Manuscript symbol   | Meaning                                    | Units               |
| ------------ | --------------------| ------------------------------------------ | ------------------- |
| `D_mu_vals`  | $$\Delta_\mu$$      | quenched/background Gaussian field width   | $$\mu{\rm s}^{-1}$$ |
| `D_Li_vals`  | $$\Delta_{\rm Li}$$ | Li-driven dynamical field width            | $$\mu{\rm s}^{-1}$$ |
| `nu_mu_vals` | $$\nu_\mu$$         | muon hopping/background decorrelation rate | $$\mu{\rm s}^{-1}$$ |
| `nu_Li_vals` | $$\nu_{\rm Li}$$    | Li-driven fluctuation rate                 | $$\mu{\rm s}^{-1}$$ |
| `alpha_vals` | $$\Lambda$$         | retarded backaction/memory strength        | $$\mu{\rm s}^{-2}$$ |
| `B_L_vals`   | $$B_L$$             | applied longitudinal field                 | G (Gauss)           |
| `t_array`    | $$t$$               | time grid                                  | $$\mu{\rm s}$$      |
| `G_table`    | $$G_z(t)$$          | longitudinal muon-spin polarization        | dimensionless       |

Important: the internal name `alpha_vals` corresponds to the manuscript parameter $$\Lambda$$. The name `alpha` is retained only because 
it was used in the original numerical table-generation code.

## Static-muon convention

The lookup table assumes

$$
\nu_\mu = 0.
$$

Therefore, `nu_mu_vals` contains only one value:

```text
[0.0]
```

In this convention, `D_mu_vals` should be interpreted as the quenched/background Gaussian width (\Delta_\mu). There is no additional independent static width in this table.

The Li-driven fluctuating channel is controlled by `D_Li_vals` and `nu_Li_vals`.

## Array structure

The main table has shape

```text
G_table.shape = (10, 10, 1, 10, 10, 4, 18001)
```

The axis order is

```python
G_table[
    i_D_mu,
    i_D_Li,
    i_nu_mu,
    i_nu_Li,
    i_alpha,
    i_B_L,
    i_t
]
```

or, in manuscript notation,

$$
G_z =
G_z(
\Delta_\mu,
\Delta_{\rm Li},
\nu_\mu,
\nu_{\rm Li},
\Lambda,
B_L;
t
).
$$

Since $$\nu_\mu=0$$, the third index has length one.

## Parameter ranges

```text
D_mu_vals  : 0.001 to 1.0        microsecond^{-1}
D_Li_vals  : 0.001 to 1.0        microsecond^{-1}
nu_mu_vals : 0.0                 microsecond^{-1}
nu_Li_vals : 0.001 to 10.0       microsecond^{-1}
alpha_vals : 0.0001 to 0.1       microsecond^{-2}
B_L_vals   : [0, 5, 10, 30]      G
t_array    : 0.0 to 18.0         microsecond, step 0.001 microsecond
```

## Minimal loading example

```python
import numpy as np

data = np.load("dataset_mu_Li_OU_alpha_Ntraj_90k.npz")

t = data["t_array"]
G = data["G_table"]

print(data.files)
print(G.shape)
```

To extract one curve,

```python
i_D_mu = 7
i_D_Li = 0
i_nu_mu = 0
i_nu_Li = 0
i_alpha = 0
i_B_L = 0

Gz = G[i_D_mu, i_D_Li, i_nu_mu, i_nu_Li, i_alpha, i_B_L, :]
```

This example selects a zero-field curve dominated by the quenched/background width $$\Delta_\mu$$, giving a KT-like polarization profile.

## Notes

* The table values are dimensionless polarization functions $$G_z(t)$$.
* The time axis is in microseconds.
* Field widths and fluctuation rates are in $$\mu{\rm s}^{-1}$$.
* The memory/backaction parameter stored as `alpha_vals` corresponds to $$\Lambda$$ in the manuscript and has units $$\mu{\rm s}^{-2}$$.
* The table was generated using $$N_{\rm traj}=90{,}000$$ stochastic trajectories.

